# 象棋陪練第一版：設計與實作計畫

## 目標

讓初學者在 Windows 本機瀏覽器與電腦下棋。當使用者走出明顯失誤時，教練先提示觀察方向，讓使用者選擇重走或繼續；對局後可重練自己的失誤局面。

完成標準：能合法走完一局；引擎能在限定時間內應手；明顯失誤會得到有棋盤依據的中文提示；對局、失誤和複習結果能保存，重新啟動後仍在。

## 第一版功能

1. **陪練對局**：選紅黑方、入門／普通／挑戰難度；點擊棋子和目的地走棋；顯示合法位置、上一手、棋步及勝負。
2. **走後指導**：分析使用者落子前後的局面。只在評分差距顯著時介入，並優先指出可核對的直接威脅。引擎資料不充分時給保守提示。
3. **分層提示與重走**：第一層提醒觀察方向，第二層標亮關鍵格子，第三層顯示建議走法及對手可能回應。使用者可重走，也可接受原走法繼續。
4. **對局紀錄與複盤**：保留每一步的局面及失誤，能回看棋盤和提示。
5. **我的錯題**：從自己的失誤局面作答，記錄重練次數與獨立答對次數。
6. **學習概況**：顯示已下對局、待練錯題、重練正確率與常見失誤類型。

第一版的提示聚焦於直接戰術：失子、漏看將軍與明顯的強制回應。開局原則、複雜局勢評語和自由對話留待取得可靠範例後擴充。

## 畫面

- **首頁**：繼續對局、開始新局、我的錯題、最近對局。
- **陪練頁**：棋盤居左、教練與棋步居右；窄螢幕時改為上下排列。預設不顯示引擎原始分數。
- **複盤頁**：依棋步回看局面、自己的走法、建議走法和當時的提示。
- **錯題頁**：顯示失誤發生前的局面，先讓使用者作答，再顯示建議與結果。
- **學習概況頁**：少量清楚的進步指標。

## 技術結構

遵循 `WEBSITE_DESIGN_AND_PYTHONANYWHERE_GUIDE.md` 的 Flask 模式：

- `create_app()` 與可匯入的 `app`；本機啟動和 WSGI 共用同一套程式。
- Jinja 共用版型、原生 CSS、少量原生 JavaScript；內部連結使用 `url_for()`。
- 路由、棋規、UCCI 通訊、教練判斷和資料庫分成獨立模組。
- SQLite 存在可寫的 `instance/`；設定透過環境變數或 factory 測試設定注入。
- Windows 啟動器僅監聽 `127.0.0.1` 並開啟瀏覽器。

棋盤狀態使用 FEN，走法使用 UCCI 座標。棋規模組負責合法走法和將軍判定；引擎模組負責有限時的 `position`、`go`、`info`、`bestmove` 交換。教練模組以使用者視角比較走前最佳評分與走後評分，並核對引擎變化中的將軍／吃子，再產生中文提示。

引擎路徑可設定。本機先使用已實測的 ElephantEye 3.21；未來 Linux 可替換為已交叉編譯的 3.31 版本。首次對外部署前另做 Linux 執行測試、登入及正式環境設定。

## 開發順序

1. 建立 Flask 專案骨架、設定、資料庫、Windows 啟動器與基本版型。
2. 實作棋盤 FEN、合法走法、將軍與勝負；用代表性棋局測試。
3. 封裝 UCCI 引擎並測試開局與非開局局面、逾時和程序退出。
4. 完成新局、落子、引擎應手及棋盤互動。
5. 加入失誤判斷、分層提示、重走與接受原走法。
6. 加入對局複盤、錯題重練、學習概況。
7. 用瀏覽器跑完整流程，檢查重新啟動後資料保留與錯誤恢復。

## 驗收重點

- 棋規阻止非法走法，包含蹩馬腿、塞象眼、炮架、將帥照面及被將軍後的應將。
- 引擎給出的走法再次通過棋規驗證；引擎失敗時使用者看到可理解的錯誤，棋局仍可恢復。
- 教練提示的攻擊、吃子或將軍都能在局面中重現；不確定時不用肯定語氣聲稱戰術原因。
- 重走後局面不前進；接受原走法後棋步、局面與錯題一致。
- 重新啟動程式後對局、錯題和學習紀錄仍可讀取。
- Flask factory 可用暫存資料庫測試；頁面與靜態網址可在 WSGI 子路徑產生正確連結。

## 已知界線

第一版以標準合法走法和基本勝負為主。長將、長捉等競賽細則的裁定需要專門案例驗證，會獨立標示與補強。淺層引擎分數可能波動，因此提示門檻採保守設定，避免把可行走法誤判為失誤。

## 大師帶走模式（第二階段）

- 對局可選「自主練習」或「大師帶走」，並可在對局頁切換；舊對局預設自主練習。
- 大師帶走在每次輪到使用者時分析局面，於真實棋盤標示一個建議走法，並給出可核對的應將、吃子或將軍原因；其他局面明確標示為引擎在目前深度的建議。
- 獨立推演棋盤播放最多六個半步，提供前進、後退與關閉。每一步都用棋規驗證，推演不寫入對局資料庫。
- 對方回應標為「可能」，避免把單一引擎變化當成唯一結果。引擎超時時可重新分析，真實棋局仍可操作。

## 原因講解與走法比較（第三階段）

- 將講解拆成「眼前問題」和「這步作用」；偵測將軍者，並在炮將軍時核對炮架數量。直線將軍在棋盤上顯示攻擊路線。
- 點選「比較我想走的棋」後，使用者可在棋盤選一個合法走法。後端分別搜尋推薦走法與所選走法的後續，獨立推演畫面可切換兩條分支。
- 比較說明優先指出對手下一步能否直接吃子或將軍。搜尋分數接近或不穩定時，明確告知不能斷言哪步較差。
- 比較 API 驗證局面版本與每一步合法性，分析結果不寫入對局與錯題資料。

## 看懂對手走法（新手教學）

- 大師帶走模式中，每次電腦走完先問「電腦這步想做什麼？」；可選將軍、吃子／捉子、調整位置或還看不出來。
- 答題後顯示可由棋規核對的直接效果與棋盤標示，再由使用者按鈕查看我方下一步建議；直接走棋也能跳過題目。
- 對手意圖題以將軍、已吃子、剛移動棋子的下一回合合法吃子、其他移位依序分類。捉子只描述可能的下一步，不宣稱對手必然照走。
- 答案以局號與棋步存入 SQLite；舊資料庫啟動時自動建立答題表。

## 實作進度（2026-09-20）

- [x] 步驟 1：Flask 專案、SQLite、Windows 啟動器、共用版型。
- [x] 步驟 2：FEN、合法走法、將軍和基本勝負判斷。
- [x] 步驟 3：UCCI 通訊、分析逾時與 Windows 引擎實測。
- [x] 步驟 4：紅／黑方開局、落子、電腦應手與互動棋盤。
- [x] 步驟 5：明顯失誤提醒、三層提示、重走與保留走法。
- [x] 步驟 6：複盤、錯題練習、學習概況與資料保存。
- [ ] 步驟 7：瀏覽器完整互動驗收與畫面檢查。

目前 22 項自動測試涵蓋棋規、紅／黑方開局、真實引擎應手、提示與重走、錯題作答、舊資料庫升級、大師推演、兩種走法比較及對手意圖題；推演每一步合法且不改動對局，答題也不改變棋局。本機伺服器已實際啟動，首頁與健康檢查均回傳 HTTP 200。此執行環境的 Chrome 與 Edge 無頭模式在 GPU 初始化時退出，因此尚未完成新增推演及答題畫面的實際滑鼠操作驗收；步驟 7 保留待一般瀏覽器操作確認。
